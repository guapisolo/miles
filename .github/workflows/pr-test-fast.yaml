name: PR Test Fast

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  fast-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up uv
        uses: astral-sh/setup-uv@v5
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          uv pip install sglang --system
          uv pip install -e . --system
          uv pip install pytest --system

      - name: Run fast tests
        run: |
          uv run pytest tests/fast -v \
            --ignore=tests/fast/rollout/generate_hub \
            --ignore=tests/fast/rollout/inference_rollout
